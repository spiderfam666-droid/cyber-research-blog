---
interface Props {
  title?: string;
}

const { title = 'Recent Posts' } = Astro.props;
---

<aside class="rounded-xl border border-border bg-surface-elevated p-5">
  <h2 class="mb-4 text-sm font-semibold uppercase tracking-wider text-foreground">{title}</h2>
  <slot />
</aside>
